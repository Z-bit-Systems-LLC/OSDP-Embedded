build/
build-*/
out/
cmake-build-*/
dist/

# PlatformIO's per-project working dir (toolchains, resolved lib_deps,
# firmware objects). Appears as soon as anything in-tree is built with
# `pio run` — an examples/ project, or a manifest smoke test.
.pio/

*.obj
*.o
*.lib
*.a
*.exe
*.dll
*.pdb
*.ilk
*.exp

.vs/
.vscode/
.idea/
CMakeUserPresets.json
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
compile_commands.json

target/
Cargo.lock

# Staging dir populated by scripts/Stage-Crate.ps1 immediately before
# `cargo publish`; never checked in. The C source tree is the canonical
# copy at `<repo>/{core,pd,acu}/`; vendor-c/ is just a transient mirror
# so the published .crate ships them. The README is the same story:
# canonical at the repo root, copied into rust/osdp/ at publish time
# so cargo resolves the `readme = "README.md"` field.
rust/osdp/vendor-c/
rust/osdp/README.md

.claude/

*~
*.swp
.DS_Store
Thumbs.db
